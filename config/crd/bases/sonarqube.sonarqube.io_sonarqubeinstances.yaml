---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sonarqubeinstances.sonarqube.sonarqube.io
spec:
  group: sonarqube.sonarqube.io
  names:
    kind: SonarQubeInstance
    listKind: SonarQubeInstanceList
    plural: sonarqubeinstances
    singular: sonarqubeinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SonarQubeInstance is the Schema for the sonarqubeinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SonarQubeInstanceSpec defines the desired state of SonarQubeInstance
            properties:
              adminSecretRef:
                description: 'adminSecretRef is the name of the Secret containing
                  the admin password (key: password).'
                minLength: 1
                type: string
              database:
                description: database contains the connection configuration for the
                  PostgreSQL database.
                properties:
                  host:
                    description: host is the hostname of the PostgreSQL server.
                    minLength: 1
                    type: string
                  name:
                    description: name is the PostgreSQL database name.
                    minLength: 1
                    type: string
                  port:
                    default: 5432
                    description: port is the PostgreSQL port. Defaults to 5432.
                    format: int32
                    type: integer
                  secretRef:
                    description: secretRef is the name of the Secret containing POSTGRES_USER
                      and POSTGRES_PASSWORD.
                    minLength: 1
                    type: string
                required:
                - host
                - name
                - secretRef
                type: object
              edition:
                default: community
                description: edition is the SonarQube edition to deploy.
                enum:
                - community
                - developer
                - enterprise
                type: string
              ingress:
                description: ingress configures the optional Ingress resource.
                properties:
                  enabled:
                    default: false
                    description: enabled controls whether an Ingress is created.
                    type: boolean
                  host:
                    description: host is the hostname for the Ingress rule (e.g. "sonarqube.example.com").
                    type: string
                type: object
              jvmOptions:
                description: jvmOptions are extra JVM flags passed to SonarQube (e.g.
                  "-Xmx2g -Xms1g").
                type: string
              persistence:
                description: persistence configures the PersistentVolumeClaim for
                  SonarQube data.
                properties:
                  size:
                    default: 10Gi
                    description: size is the requested storage size (e.g. "10Gi").
                    type: string
                  storageClass:
                    description: storageClass is the name of the StorageClass to use.
                    type: string
                type: object
              resources:
                description: resources defines the CPU and memory requests/limits
                  for the SonarQube container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              version:
                description: version is the SonarQube image tag to deploy (e.g. "10.3").
                minLength: 1
                type: string
            required:
            - adminSecretRef
            - database
            - edition
            - version
            type: object
          status:
            description: SonarQubeInstanceStatus defines the observed state of SonarQubeInstance.
            properties:
              conditions:
                description: conditions represent the detailed state of the SonarQubeInstance.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: phase is the current lifecycle phase of the instance.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                type: string
              url:
                description: url is the internal cluster URL of the SonarQube service.
                type: string
              version:
                description: version is the SonarQube version currently running.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
