---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sonarqubeusers.sonarqube.sonarqube.io
spec:
  group: sonarqube.sonarqube.io
  names:
    kind: SonarQubeUser
    listKind: SonarQubeUserList
    plural: sonarqubeusers
    singular: sonarqubeuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceRef.name
      name: Instance
      type: string
    - jsonPath: .spec.login
      name: Login
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SonarQubeUser is the Schema for the sonarqubeusers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SonarQubeUserSpec defines the desired state of SonarQubeUser.
            properties:
              email:
                description: email is the email address of the user.
                type: string
              groups:
                description: |-
                  groups is the list of SonarQube groups this user should belong to.
                  When non-empty, the operator manages group membership declaratively:
                  it adds missing groups and removes groups that were previously managed
                  by the operator but are no longer listed here.
                  Groups assigned by other means are never removed.
                items:
                  type: string
                type: array
              instanceRef:
                description: instanceRef references the target SonarQubeInstance.
                properties:
                  name:
                    description: name est le nom de la SonarQubeInstance.
                    minLength: 1
                    type: string
                  namespace:
                    description: 'namespace est le namespace de la SonarQubeInstance.
                      Défaut : même namespace que le plugin.'
                    type: string
                required:
                - name
                type: object
              login:
                description: login is the unique and immutable login of the user in
                  SonarQube.
                minLength: 2
                type: string
                x-kubernetes-validations:
                - message: spec.login is immutable after creation
                  rule: self == oldSelf
              name:
                description: name is the display name of the user.
                minLength: 1
                type: string
              passwordSecretRef:
                description: |-
                  passwordSecretRef references a Secret containing the user's initial password
                  under the key "password". Only used when creating a local SonarQube user.
                  If omitted, SonarQube generates a random password (user must reset via email).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              scmAccounts:
                description: |-
                  scmAccounts is the list of SCM identities (Git committer emails / names)
                  linked to this user, used to attribute analysis findings correctly.
                  Reconciled with set semantics: SonarQube's SCM account list is replaced
                  by this list on each reconcile. Pass an empty list to clear all accounts.
                items:
                  type: string
                type: array
            required:
            - instanceRef
            - login
            - name
            type: object
          status:
            description: SonarQubeUserStatus defines the observed state of SonarQubeUser.
            properties:
              active:
                description: active reflects whether the user account is active in
                  SonarQube.
                type: boolean
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              groups:
                description: |-
                  groups is the list of groups last synchronized by the operator.
                  Used to detect which groups should be removed if they are no longer in spec.groups.
                items:
                  type: string
                type: array
              phase:
                description: phase is the current state of the user.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
