# Quality Gates as Code

!!! info "Coming soon"
    How to define `SonarQubeQualityGate` resources, set conditions, mark a gate
    as default, and rely on drift detection to keep teams from changing gates
    manually.
