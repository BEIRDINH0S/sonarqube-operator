# SonarQubeInstance

!!! info "Coming soon"
    Full schema reference for `SonarQubeInstance` — every field of `spec` and
    `status`, the conditions vocabulary, and an example covering every option.
