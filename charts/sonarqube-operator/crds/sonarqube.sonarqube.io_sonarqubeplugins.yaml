---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sonarqubeplugins.sonarqube.sonarqube.io
spec:
  group: sonarqube.sonarqube.io
  names:
    kind: SonarQubePlugin
    listKind: SonarQubePluginList
    plural: sonarqubeplugins
    singular: sonarqubeplugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceRef.name
      name: Instance
      type: string
    - jsonPath: .spec.key
      name: Key
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.installedVersion
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SonarQubePlugin is the Schema for the sonarqubeplugins API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SonarQubePlugin
            properties:
              instanceRef:
                description: instanceRef référence la SonarQubeInstance sur laquelle
                  installer le plugin.
                properties:
                  name:
                    description: name est le nom de la SonarQubeInstance.
                    minLength: 1
                    type: string
                  namespace:
                    description: 'namespace est le namespace de la SonarQubeInstance.
                      Défaut : même namespace que le plugin.'
                    type: string
                required:
                - name
                type: object
              key:
                description: key is the unique and immutable plugin key in the SonarQube
                  marketplace (e.g. "sonar-java").
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: spec.key is immutable after creation
                  rule: self == oldSelf
              version:
                description: version est la version du plugin à installer. Si omis,
                  la dernière version est installée.
                type: string
            required:
            - instanceRef
            - key
            type: object
          status:
            description: status defines the observed state of SonarQubePlugin
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              installedVersion:
                description: installedVersion est la version du plugin actuellement
                  installée.
                type: string
              phase:
                description: phase est l'état courant du plugin.
                enum:
                - Pending
                - Installing
                - Installed
                - Failed
                type: string
              restartRequired:
                description: restartRequired indique que SonarQube doit redémarrer
                  pour appliquer les changements.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
