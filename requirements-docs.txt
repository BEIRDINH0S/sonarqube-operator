# Pinned versions for reproducible documentation builds.
# Bump these intentionally — Material releases ship occasional breaking changes.
mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocs-material-extensions==1.3.1
pymdown-extensions==10.12
# Markdown 3.8.x silently breaks pymdownx.superfences (fenced blocks render
# as inline <code> spans). 3.9+ fixes the regression — keep this pin.
Markdown>=3.9
# Pygments 2.20 has a regression that breaks pymdownx.superfences fence
# detection (every ```lang block renders as inline <code>...</code>). Stay
# on 2.19.x until pymdown-extensions ships a compatible release.
Pygments>=2.18,<2.20
